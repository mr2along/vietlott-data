Strategy Comparison Report
==================================

This file is a compatibility marker only.

The historical values previously stored here came from the legacy backtest
pipeline and are not a canonical Power 6/55 benchmark because that pipeline
mixed the special number with the six main numbers and used an incompatible
prize mapping.

Use artifacts/backtest_v2/ and the Backtest V2 Final GitHub Actions workflow
for current research results.
